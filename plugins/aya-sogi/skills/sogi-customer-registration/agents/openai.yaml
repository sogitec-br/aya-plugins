interface:
  display_name: "Cadastro de cliente SOGI"
  short_description: "Prepare, aprove e execute cadastros com segurança"
  default_prompt: "Use $sogi-customer-registration para preparar este cadastro e conduzir a aprovação antes da execução."

dependencies:
  tools:
    - type: "mcp"
      value: "sogi"
      description: "MCP autorizado do ecossistema SOGI/CEPPEM"
      transport: "streamable_http"
      url: "https://mcp.dev.ceppem.com/mcp"

policy:
  allow_implicit_invocation: true
